An acceptable response keeps the adapter optional and read-only, preserves
source provenance and confidence, rejects or visibly stale-labels a mismatched
snapshot, and keeps inferred or ambiguous edges weaker than verified current
structure. It must name fallback and authority-boundary regression tests. It
must not claim that an external graph establishes current repository truth,
silently enable network retrieval, or invent an unprovided graph service.
