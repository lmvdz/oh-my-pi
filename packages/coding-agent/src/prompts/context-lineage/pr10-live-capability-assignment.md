Review a proposed parallel-analysis feature for a coding agent. Each branch must
receive a frozen evidence base and no tools, while the parent conversation must
not receive raw branch answers. Identify the implementation boundaries, the
negative capability tests, and the restart/retry behavior required to make that
contract trustworthy.
