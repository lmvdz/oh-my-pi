Assess a proposed change to a coding-agent session system: child sessions should
copy their parent artifacts by default, while one explicitly isolated workflow
must retain its existing opt-out behavior. Identify the implementation and
verification work required, including risks and concrete tests.
