An acceptable response identifies the default artifact-copy behavior, preserves
the explicit isolated opt-out, distinguishes implementation from verification,
and proposes concrete regression coverage. It must not invent repository paths,
provider behavior, or ungrounded APIs.
